This directory is a copy of ../full-package-1/build/
We first had to write `homepage:` property in package.json;
After running `npm run build`, we `sudo cp build/* /my/other/dir/`;

Our next hope is to have this work with a bundle of some sort;
